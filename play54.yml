---
 - name: demonstate the iteration statements
   hosts: all
   gather_facts: no
   become: yes
   remote_user: ansible
   tasks:
    - name: task to repeat foe 5 times
      debug: 
       msg: "helo...every one...{{item}}"
     # loop: [1,2,3,4,5]
      loop:
       - sree
       - giri
...
