---
 - name: install package
   hosts: 192.168.1.99
   become: yes
   remote_user: ansible
   tasks:
    - name: include vars in tasks
      include_vars: vars/dbvars.yml
    - name: install package
      include: tasks/inst_pck.yml
    - name: start service
      include: tasks/start_ser.yml
...