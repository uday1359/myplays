---
 - name: play to demonstrate magic variables
   hosts: all
   become: true
   tasks:
    - name: print groups 
      debug:
       var: groups
    - name: print inventory_hostname
      debug: 
       var: inventary_hostname
    - name: print group_names
      debug:
       var: group_names
    #- name: print hostvars
    #  debug: 
    #  var: hostvars
...
