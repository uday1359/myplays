---
 - name: 
   hosts: 192.168.1.99
   become: yes
   remote_user: ansible
   tasks: 
    - name: 
      find: 
       paths: 
        - /var
        - /etc
        - /usr
       patterns: 'httpd'
      register: out
    - name: 
      debug: 
       msg: "{{item.path}}"
      loop: out.files
...
